package com.hardware.auth.domain

import org.springframework.stereotype.Service

//const secret = process.env.JWT_SECRET

@Service
class TokenService {
//    val jwt = require('jsonwebtoken')
//    val bcrypt = require('bcrypt')
//
//
//
//    fun sign(payload: Any){
//        return jwt.sign(payload, secret, {expiresIn: '60m'})
//    }
//
//    fun verify(bearToken: String){
//        var token = bearToken && bearToken.slice(7, bearToken.length).trimLeft()
//        return jwt.verify(token, secret)
//    }

}